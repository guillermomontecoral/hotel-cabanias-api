@model LogicaNegocio.Entidades.Cabanha

@{
    ViewData["Title"] = "Agregar más fotos";
}

<h1>Agregar más fotos</h1>

<hr />
<div class="mt-5">
    EN CONSTRUCCIÓN...
</div>
<div class="mt-3 col">
    <a asp-controller="Cabanha" asp-action="Details" asp-route-id="@Model.Id">Volver a la lista</a>
</div>
<div class="mt-3">
    <p class="text-danger">@ViewBag.Error</p>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
